using UnityEngine;
public class UIscripts : MonoBehaviour


{


    /*
   private void OnGUI()
    {
        int playerScore = LevelManager.instance.GetPlayerScore();

        string scoreText = "Player Score: " + playerScore;


        GUI.contentColor = Color.white;
        GUILayout.BeginArea(new Rect(50f, 10f, 1600f, 1600f));
        GUILayout.Label($"<size=24>{scoreText}</size>");
        GUILayout.EndArea();

        string highScoreText = "Player High Score: " + LevelManager.instance.GetHighScore();

        GUI.contentColor = Color.white;
        GUILayout.BeginArea(new Rect(50f, 30f, 1600f, 1600f));
        GUILayout.Label($"<size=24>{highScoreText}</size>");
        GUILayout.EndArea();

        string timesPlayedText = "You have played this game " + LevelManager.instance.GetTimesPlayed()+ " times.";

        GUI.contentColor = Color.red;
        GUILayout.BeginArea(new Rect(50f, 1050f, 1600f, 1600f));
        GUILayout.Label($"<size=24>{timesPlayedText}</size>");
        GUILayout.EndArea();
    }

    */
}
